#include <stdio.h>

#define MAX_TXT 256
int main(int argc, char *argv[]) {

    if(argc != 4){
        printf("Uso: %s <max>, <min>, <esperado>\n", argv[0]);
        return 1;
    }

    

    return 0;
}