#include <stdio.h>
#include <math.h>

int main() {
    float baseMaior = 2;
    float baseMenor = 3;
    float altura = 2;
    float area;

    area = (baseMaior + baseMenor) * altura / 2;

    printf("A area do trapezio eh %.2f\n", area);

    return 0;
};

//teste 
