#include <stdio.h>
#include <math.h>

int main() {
    float base = 2;
    float altura = 3;
    float area;

    area = base * altura;

    printf("A area do retangulo eh %.2f\n", area);

    return 0;
};
